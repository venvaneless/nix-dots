# /Users/ven/dotfiles/nix/hosts/linux/system.nix
#
# LINUX: SYSTEM CONFIG
# System-level NixOS configuration placeholder:
#   - Future system defaults
#   - Future services and paths
# ============================================================

{ config, pkgs, lib, ... }:

{
  # ------------------------------------------------------------
  # LINUX SYSTEM CONFIG
  # Empty for now; extend as NixOS system grows.
  # ------------------------------------------------------------
}
