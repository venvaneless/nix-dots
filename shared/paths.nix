# /Users/ven/dotfiles/nix/shared/paths.nix
#
# SHARED: PATH SETTINGS
# Placeholder for future shared PATH-related helpers.
# Not wired yet to avoid interfering with hardcoded paths.
# ============================================================

{ ... }:

{
  # ------------------------------------------------------------
  # SHARED PATH CONFIG
  # Empty for now; extend with shared PATH logic later.
  # ------------------------------------------------------------
}
